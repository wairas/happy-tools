# rl-manual

User has to manually locate the reference file.

```
usage: rl-manual [-h]

User has to manually locate the reference file.

optional arguments:
  -h, --help  show this help message and exit
```
